import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fayyfepp_version_2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
